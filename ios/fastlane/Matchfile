git_url(ENV["MATCH_GIT_URL"] || "https://github.com/shunseiw-ctrl/certificates.git")

storage_mode("git")

type("appstore")

app_identifier(["com.albawork.app"])

# For more information about the Matchfile, see:
#     https://docs.fastlane.tools/actions/match/
