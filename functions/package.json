{
  "name": "functions",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "lint": "eslint src/ index.js",
    "test": "jest --coverage --testPathIgnorePatterns=tests/rules/",
    "test:watch": "jest --watch --testPathIgnorePatterns=tests/rules/",
    "test:rules": "firebase emulators:exec --only firestore 'jest tests/rules/ --forceExit'"
  },
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.0.5",
    "stripe": "^14.0.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "eslint": "^8.57.0",
    "eslint-config-google": "^0.14.0",
    "firebase-functions-test": "^3.4.0",
    "jest": "^29.7.0"
  }
}
